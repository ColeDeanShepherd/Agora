{
  "name": "agora",
  "version": "0.1.0",
  "description": "A bare-bones Hello World Electron app",
  "main": "dist/main.js",
  "scripts": {
    "build": "rm -rf dist && tsc && mkdir -p dist/renderer && cp -r src/renderer/. dist/renderer",
    "start": "npm run build && electron ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "electron": "^33.0.0",
    "typescript": "^5.0.0"
  }
}
